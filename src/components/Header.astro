---

---

<header>
  <h1>EPC</h1>
  <ul>
    <li><a href="/">Top</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
</header>
<style lang="scss">
  header {
    display: flex;
    align-items: center;
    padding: 1rem 2rem;
    background-color: #f4f4f4;
    h1 {
      font-size: 2rem;
      font-weight: 300;
      border-top: #29009b 2px solid;
      height: 50px;
      position: relative;
      &::before {
        content: "";
        position: absolute;
        width: 70.710678118655px;
        height: 0;
        top: 45%;
        left: 85%;
        border-top: #29009b 2px solid;
        transform: rotate(45deg);
      }
    }

    ul {
      display: flex;
      list-style: none;

      li {
        position: relative; // 追加: 疑似要素の位置を制御するため
        margin-left: 3rem;

        // 追加: ::beforeと::after疑似要素を使用して斜めの線を作成
        &::before,
        &::after {
          content: "";
          position: absolute;
          width: 0;
          height: 100%;
          border: #29009b 1px solid;
          border-radius: 1px;
        }

        // 追加: ::before疑似要素の詳細
        &::before {
          transform-origin: 0 0;
          transform: rotate(45deg);
        }

        // 追加: ::after疑似要素の詳細
        &::after {
          transform-origin: 1px 0;
          transform: rotate(-45deg);
        }

        a {
          padding: 0 10px;
          border-top: #29009b 2px solid;
          text-decoration: none;
          color: #333;
          &:hover {
            color: #f1356d;
          }
        }
      }
    }
  }
</style>
